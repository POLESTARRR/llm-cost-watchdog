# --- Provider SDKs ---------------------------------------------------------
anthropic==0.120.2
openai==2.53.0
google-generativeai==0.8.6

# --- Core ------------------------------------------------------------------
mcp==2.0.0
pydantic==2.13.4
python-dotenv==1.2.2
httpx==0.28.1  # also used by scripts/import_claude_code_usage.py --remote-url

# Deliberately NOT listing `libsql` here (see src/turso_backend.py). Its
# only prebuilt wheels are macOS and manylinux x86_64, no linux/arm64, no
# macOS cp314, so it fails to install from source on an unrelated dev
# machine (confirmed: this exact repo's own venv, Python 3.14 on Apple
# Silicon, has no wheel and no Rust toolchain to build one). It's installed
# separately in Dockerfile, which targets linux/amd64 (Render's real
# platform) where a real wheel exists. Local dev and tests never need it --
# TURSO_DATABASE_URL is unset, so src/turso_backend.py is never imported.

# --- Dashboard -------------------------------------------------------------
fastapi==0.141.1
uvicorn==0.52.1

# --- Tests -----------------------------------------------------------------
pytest==9.1.1
